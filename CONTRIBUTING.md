# Contributing

Open an issue before a substantial change. Keep checks deterministic, path-based, and advisory. Add tests for every rule, run `pytest -q`, and never commit credentials or webhook payloads.
